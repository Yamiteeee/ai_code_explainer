/// A simple data models to hold the extracted code and its explanation.
class ExplanationResult {
  /// The text recognized from the image.
  final String extractedCode;

  /// The explanation generated by the AI models.
  final String explanation;

  /// Constructor for ExplanationResult.
  ExplanationResult({
    required this.extractedCode,
    required this.explanation,
  });

  /// Factory constructor to create an empty or initial state result.
  /// Used to provide default values before actual processing.
  factory ExplanationResult.empty() {
    return ExplanationResult(
      extractedCode: 'No text extracted yet.',
      explanation: 'Upload an image of code to get an explanation!',
    );
  }
}
